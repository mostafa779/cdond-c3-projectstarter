---
- name: "create backend-app dir"
  file:
    path: ~/backend-app
    state: directory

- name: "unarchive backend files"
  unarchive:
    src: "../../../../../artifact.tar.gz"
    dest: "~/backend-app"

- name: "execute app with pm2"
  shell: |
    cd ~/backend-app
    npm install
    cd ./dist
    pm2 stop default
    pm2 start main.js

  